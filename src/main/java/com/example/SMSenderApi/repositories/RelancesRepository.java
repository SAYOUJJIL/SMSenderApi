package com.example.SMSenderApi.repositories;

import com.example.SMSenderApi.entities.Relances;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelancesRepository extends JpaRepository<Relances,Long> {
}
